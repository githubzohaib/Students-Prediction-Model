# The committed model artifacts must survive a Windows -> Linux checkout
# byte for byte. core.autocrlf is on, so mark them binary explicitly rather
# than relying on git's content heuristic -- a single CRLF rewrite makes a
# pickle unloadable.
*.pkl  binary
*.npy  binary
*.csv  -text
